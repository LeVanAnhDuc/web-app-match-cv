/** Document input mode for the wizard's Upload/Paste switcher. */
export type InputMode = "upload" | "paste";

/** Current wizard step (1: JD, 2: CV, 3: Review, 4: Result). */
export type WizardStep = 1 | 2 | 3 | 4;
