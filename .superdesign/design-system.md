# web-app-match-cv — Design System (STRICT — SuperDesign MUST obey)

> _TBD — sync từ `.claude/uiux/` khi có design token. Giữ cơ chế strict-theme (root `.claude/CLAUDE.md` §3.3): `<head>` boilerplate map token → CSS var, cấm palette generic + cấm Google Font, luôn xuất light + dark._

## 0. MANDATORY BOILERPLATE — copy this `<head>` VERBATIM into every generated HTML

## 1a. BOTH themes — MANDATORY page structure

## 1b. HARD RULES (a design is REJECTED if it breaks any)

## 2. Typography tiers

## 3. Header (reproduce faithfully — see replica template)

## 4. (feature-specific sections)

## 5. UX copy

## 6. Accessibility
